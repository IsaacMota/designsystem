import 'package:flutter/material.dart';

class LinkedLabelSampleScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: LinkedLabelSampleScreen(), // Exibe o widget LinkedLabel
    );
  }
}
